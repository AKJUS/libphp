{
  "name": "libphp-php-monorepo",
  "devDependencies": {
    "@nx/js": "^22.3.3",
    "nx": "^22.3.3"
  },
  "scripts": {
    "bootstrap": "nx run-many --target=install --all",
    "build": "nx run-many --target=build --all",
    "test": "nx run-many --target=test --all"
  }
}
